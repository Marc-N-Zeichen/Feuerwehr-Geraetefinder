/**
 * Konfigurationskonstanten für die Anwendung
 * @module config
 */

/**
 * Minimale Länge der Sucheingabe, bevor Ergebnisse angezeigt werden
 * @constant {number}
 */
export const MIN_SEARCH_LENGTH = 3;

/**
 * Debounce-Verzögerung für die Suche in Millisekunden
 * @constant {number}
 */
export const SEARCH_DEBOUNCE_DELAY = 300;

/**
 * Scroll-Schwellenwert für den "Nach oben"-Button in Pixeln
 * @constant {number}
 */
export const SCROLL_THRESHOLD = 20;
